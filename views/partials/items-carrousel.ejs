
<section class="carrousel">
    <% items.forEach(item => { %>
        <article class="carrousel-details"> 
            <h3>
                <a href="./detail/<%= item.id %>"> <%=item.title %></a>
            </h3>
            <img src="https://fdnd-agency.directus.app/assets/<%= item.afbeelding.id %>" alt="" width="<%= item.afbeelding.id %>" height="<%= item.afbeelding.id.height %>">

            <!-- <p>
                <%= item.id %>
            </p> -->

        </article>
    <% }) %>
</section>